// Vai repetir o código até o número 100
for (i=1;i<=100;i++){
    // Vai identificar se o número é par
    if (Number.isInteger(i/2)) {
        // Vai mostrar apenas o número par
        console.log(i)
        document.write(`<h1>${i}</h1>`)
    }
}