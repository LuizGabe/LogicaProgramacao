// Repete o código 50 vezes
for (i = 1;i <= 50; i++) {

    // Vai mostrar o número, se ele não for 10, 20, 30, 40 ou 50
    if (i != 10 && i !=20 && i !=30 && i !=40 && i !=50) {
        console.log(i)
        document.write(`<h2>${i}</h2>`)
    }
}