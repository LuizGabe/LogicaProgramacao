// Repete o código 5 vezes, com o índice indo de 1 a 5
for (i=1;i<=5;i++){
    var primeiro = i

    // Repete o código 10 vezes, com o índice de 1 a 10
    for(e=1;e<=10;e++){
        var segundo = e

        // Mostra no console os  número que estão multiplicando e o resultado da multiplicação do primeiro for com o número do segundo for
        console.log(`${primeiro} x ${segundo} = ${primeiro*segundo}`)
    }
}